# AGENTS.md — Physics Experiment Assistant 实现约束

> 本文件是本项目的工程实现约束。任何人类开发者、AI 编码代理、自动化脚本在修改本仓库时都必须遵守本文件。若实现与本文件或 `plan.md` 冲突，先停止实现并在文档中记录冲突；不得擅自“按常识修正”课程规则。

**每次开始执行任务前，都要根据用户要求仔细阅读详细思考对应的 plan.md 等计划文件，并制作详细计划再开始执行，且执行过程中每次完成一个小阶段开始下一步前都要重新阅读 plan.md 计划文件对应部分明确语义和要求防止落实误差.**

**AGENTS.md 和其他文档在必要的时候需要更新，但请注意，更新的时候要清理掉过期的旧语义和旧记录，相关文档的更新不是无限制增加章节和字数，而是动态维护时效性.**

## 1. 项目使命

本项目是一个部署在 GitHub Pages 的纯前端“大学物理实验小助手”。产品由两条主线组成：

1. **实验工作台**：针对指定实验提供从原始数据记录、数据清洗/修正、派生量计算、拟合、作图、不确定度、有效数字、结果表达直到可复制报告片段的完整流程。
2. **公式与通用计算工作台**：允许用户选择公式、填写物理量与单位、指定目标量、附加不确定度并完成计算；同时提供统计、拟合、加权平均、插值、单位换算、科学计算等常用工具。

项目的第一优先级不是“公式数量”，而是**数值正确、规则可追溯、课程模式与标准模式严格隔离、流程流畅、导出可复核**。

## 2. 资料与规则优先级

### 2.1 已阅读资料

项目设计依据以下上传资料：

- `2026秋物理实验A(1)教学资料.pdf`：当前默认课程的最高优先级资料。包含 2026 秋课程须知、测量误差与数据处理、电磁学/光学基础，以及 7 个必做实验。
- `课程基础知识202009.pptx`：绪论课/课程基础知识课件。重点用于直接/间接测量、不确定度、有效数字、作图、最小二乘及拟合参数有效位数的细化规则。
- `讲义第II部分课程基础知识.pdf`：2020 版课程基础知识讲义。用于交叉核对公式、线性拟合参数不确定度公式与旧版课程口径。
- `讲义I部分B1课程须知2020秋20200907.pdf` 与带 `(1)` 的同名文件：二者字节级相同，只视为一份历史课程须知，不重复建模。
- `GB ∕ T 27418-2017 测量不确定度评定和表示.pdf`：国家标准模式的依据。该模式与物理实验课程的教学简化模式必须隔离。

### 2.2 规则冲突处理

默认规则优先级：

1. **2026 秋物理实验 A(1) 当前讲义中的明确规则**；
2. **课程基础知识绪论课 PPT 中没有与 2026 当前讲义冲突的细化规则**；
3. 2020 版 II 部分讲义中的兼容细节；
4. 一般物理/统计知识只可作为扩展功能，必须在 UI 和数据定义中标注为“通用扩展”；
5. GB/T 27418-2017 仅在“GB/T 27418-2017”标准配置中生效，不得反向覆盖课程模式。

如果资料未明确给出某一步算法，不得把推断伪装成课程规定。必须在公式或实验定义的 `provenance.status` 中标为 `derived`、`general` 或 `source-explicit`。

## 3. 标准配置必须严格隔离

必须实现至少四个标准配置：

- `tsinghua-a1-2026`：**默认**。完整支持上传的 2026 秋 A(1) 规则及 7 个实验。
- `tsinghua-foundation-2020`：历史课程/绪论课兼容配置，用于复现 2020 讲义和 PPT 的规则表述。
- `gbt-27418-2017`：GB/T 27418-2017 标准不确定度/合成标准不确定度/扩展不确定度模式。
- `custom`：用户自定义配置，由显式参数组成，禁止隐式继承相互矛盾的规则。

**绝对禁止**在同一次计算中把课程模式的 `ΔB = Δ仪` 与 GB/T 模式的矩形分布 `u_B = a/√3` 混合，除非用户明确创建自定义配置并看到警告。

## 4. 数值计算不可破坏的原则

### 4.1 原始值与显示值分离

每个数值必须至少保存：

```ts
interface NumericDatum {
  rawText: string;          // 用户原始输入，例如 "15.0"
  valueSI: number;          // 计算用 SI 数值
  inputUnit?: string;
  significantDigits?: number;
  decimalPlace?: number;
  isExact?: boolean;        // π、定义常数、整数计数等
}
```

不得只保存 `number`，否则 `15.0` 与 `15` 的有效数字信息会丢失。

### 4.2 中间计算禁止提前修约

- 中间结果保存完整浮点/高精度计算值。
- 有效数字只作用于**显示和最终导出**，不作用于计算 DAG 的上游值。
- 任何 `toFixed()` 都不得作为数值算法的一部分。
- 对 π、e、定义常数等必须使用高精度常数或库常数，不得预先写成低精度字符串。

### 4.3 课程模式有效数字规则

`tsinghua-a1-2026` 必须默认遵守：

- 合成/总不确定度通常保留 **2 位有效数字**；若首位不小于 3，可简化为 **1 位有效数字**。
- 相对不确定度通常保留 **2 位有效数字**。
- 最终测量值的末位与不确定度末位对齐。
- 未做不确定度估算时：加减运算结果末位按参与量中最高可疑位/最低精度小数位对齐；乘除结果有效位数与参与量中最少有效数字者相同或多取 1 位。
- 绪论课 PPT 的中间结果规则：中间计算可以不修约或适当多留几位，不得任意减少；最终再统一修约。
- 直接读数应保留与仪器准确度相匹配的估读位；整刻度读数不得丢失用于表达精度的尾随 0。

### 4.4 GB/T 模式有效数字规则

`gbt-27418-2017` 必须默认遵守：

- `u(x)`、`u_c(y)`、`U` 通常最多 2 位有效数字。
- 输入/输出估计值与其不确定度末位对齐。
- 相关系数绝对值接近 1 时优先给 3 位有效数字。
- 标准不确定度与扩展不确定度的表达必须标出含义，不得用一个模糊的 `±` 替代所有概念。

### 4.5 修约策略

修约函数必须集中在 `core/sigfig`，禁止各组件自行实现。所有修约策略必须可测试、可配置。课程模式默认采用常规十进制四舍五入；任何特殊 tie-breaking 规则若未来需要，必须通过 profile 参数启用。

## 5. 不确定度引擎约束

### 5.1 课程模式

必须支持：

- 算术平均值；
- 贝塞尔样本标准偏差；
- 平均值标准偏差；
- Student-t 因子，默认包含/置信概率 0.95；
- `ΔA = t_P(ν) S_x̄ = t_P(ν) S_x / √n`，`ν=n-1`；
- `ΔB = Δ仪`；
- `Δ = √(ΔA² + ΔB²)`；
- 当 `ΔA < Δ仪/3` 时，课程模式允许简化为 `Δ = Δ仪`，但 UI 必须显示“采用课程简化规则”；
- 单次测量可取 `Δ = Δ仪`；
- 相对不确定度 `E = Δ/|x|`；
- 已定系统误差必须先修正，再报告估计值。

### 5.2 间接测量（课程模式）

默认假设上传课程讲义中的“输入量相互独立”前提：

`ΔY = sqrt(Σ (∂f/∂Xi · ΔXi)²)`。

对于积商幂形式可以显示对数微分/相对不确定度等价式，但内部应统一用雅可比/灵敏度系数计算，避免重复实现。

### 5.3 GB/T 27418-2017 模式

必须支持：

- A 类标准不确定度：重复观测均值的标准不确定度；
- B 类标准不确定度：根据输入信息与分布换算为标准差；
- 矩形分布半宽 `a`：`u=a/√3`；
- 三角分布半宽 `a`：`u=a/√6`；
- 正态分布给定包含概率/包含因子时的转换；
- 灵敏度系数 `ci = ∂f/∂xi`；
- 独立输入：`uc²(y)=Σ ci²u²(xi)`；
- 相关输入：加入协方差项或相关系数项；
- 协方差矩阵和相关系数矩阵；
- Welch–Satterthwaite 有效自由度；
- 扩展不确定度 `U = k uc` 或 `Up = tp(νeff) uc`；
- 标准不确定度与扩展不确定度的不同报告方式。

禁止在 GB/T 模式里把 Type A 直接乘 95% t 因子后再和标准不确定度形式的 Type B 合成。

## 6. 线性拟合与统计约束

必须支持：

- 普通最小二乘 `y=a+bx`；
- 可选强制过原点；
- 相关系数 `r`；
- `R²` 作为额外工程指标，但课程模式首先显示 `r`；
- 残差、残差平方和；
- 回归参数标准误与置信区间；
- 课程指定的 `ν=n-2` t 因子；
- 课程讲义/PPT 中使用的 `Sb`、`Sa`、`Δb`、`Δa` 公式；
- 加权线性拟合（通用扩展，不冒充课程必需）；
- 输入 x 误差不可忽略时，普通 OLS 页面要给出模型假设警告；未来可增加 ODR，但 v1 不伪装为已支持。

绪论课规则：未估算拟合参数不确定度时，截距 `a` 的末位至少与 `yi` 末位取齐，斜率 `b` 的有效位数至少与 `xi` 的有效位数一致，`r` 至少保留一个非 9 数字。

## 7. 物理量与单位

- 内部计算统一 SI；输入/显示可以使用常见实验单位。
- 单位转换必须维度检查；维度不相容时拒绝计算。
- 至少支持长度、面积、体积、质量、时间、角度、温度、频率、力、能量、功率、电流、电压、电阻、电容、电感、磁感应强度、热导率、比热、密度、霍尔系数、迁移率等。
- 摄氏温度与温差必须区分：温度转换存在偏置，温差转换只缩放。
- 无量纲量、百分数、弧度必须有明确语义。

## 8. 公式系统约束

公式不得散落在 React JSX 中。所有公式由版本化注册表驱动。

```ts
interface FormulaDefinition {
  id: string;
  version: number;
  title: string;
  aliases: string[];
  category: string;
  latex: string;
  expression: string;             // 解析后的安全表达式来源
  variables: VariableDefinition[];
  constants?: ConstantDefinition[];
  solveFor: string[];
  constraints?: FormulaConstraint[];
  uncertainty?: UncertaintyCapability;
  provenance: Provenance;
  examples?: FormulaExample[];
}
```

`provenance.status` 必须为：

- `source-explicit`：资料直接给出；
- `source-derived`：由资料给出的关系直接推导；
- `general`：通用大学物理扩展；
- `experimental`：尚未经过充分测试，不得默认展示。

禁止使用 `eval()`、`new Function()` 执行公式。使用安全 AST 解析器。

## 9. 实验工作台定义

每个实验必须由声明式定义驱动：

```ts
interface ExperimentDefinition {
  id: string;
  title: string;
  courseProfile: string[];
  provenance: Provenance;
  safety: SafetyNotice[];
  metadataFields: FieldDefinition[];
  steps: ExperimentStep[];
  datasets: DatasetDefinition[];
  computationGraph: ComputationNode[];
  plots: PlotDefinition[];
  resultBlocks: ResultDefinition[];
  exportSections: ExportSection[];
}
```

当前 2026 A(1) 必须完整覆盖：

1. 摩擦系数测量；
2. 霍尔效应及磁电阻测量；
3. 准稳态法测不良导体导热系数和比热；
4. 阻尼振动和受迫振动；
5. 示波器原理和使用、声速测量；
6. 透镜焦距测量；
7. 迈克尔逊干涉实验。

实验工作台不允许自动编造原始数据、补齐缺失测量或替用户伪造“实验小结”。它可以生成计算过程、图表、格式化结果和基于已输入数据的客观诊断。

## 10. 2026 七实验必须支持的关键计算

### 10.1 摩擦系数

- `W=M_W g`、`P=M_P g`；
- 区间测量：中心值 `(X+ + X-)/2`，`ΔX=|X+−X-|/2`；
- 固定 θ 的 P–W 图/拟合；
- 固定 W 的 P–θ 图及可选 `ln(P/W)` 线性化；
- Capstan 关系 `P=W exp(-μθ)` 作为**由实验任务意图和通用模型推导**的公式，来源状态标记 `source-derived`，不得标记为讲义原式；
- C 部分未知质量/摩擦系数的解算要显示方程来源与方向约定。

### 10.2 霍尔/磁阻

- `UH=(U1-U2+U3-U4)/4`；
- `UH=KH I B = RH I B/d`；
- `RH=1/(ne)`（课程实验近似 Hall 因子 A≈1）；
- `KH=RH/d`；
- 固定 B 下 `UH-I` 拟合；
- 由拟合斜率求 `KH`、`RH`、`n`；
- 由已标定 `KH` 与固定 I 求 B，绘制 `B-IM`；
- `R(B)=UAC/IAC`，`ΔR/R(0)=[R(B)-R(0)]/R(0)`；
- 弱磁场 `ΔR/R(0)` 对 `B²`，强磁场对 `B` 的分析。

### 10.3 准稳态热导

- 热电偶斜率 40 μV/℃；
- 初始 U1 非零作为已定系统误差修正；
- 前后加热电压平均；
- `qc=U_heat²/(2Fr)`；
- `Δt=qc R/(2λ)` 与 `λ=qc R/(2Δt)`；
- `qc F = c ρ R F · dt/dτ`，故 `c=qc/[ρ R (dt/dτ)]`；
- 准稳态区间交互选择，U1 近似恒定、U2 对时间近似线性；
- 85% 电功率修正情形重新计算 λ、c。

### 10.4 阻尼/受迫振动

- `ωd=sqrt(ω0²-β²)`、`Td=2π/ωd`；
- `ζ=β/ω0`、`τ=1/β`、`Q=1/(2ζ)`、对数减缩率；
- `ln θj` 对 `j` 线性拟合；
- 由斜率 b 求 ζ；
- `Δb=t0.95(n-2) Sb`；
- `ω0=2π/[Td sqrt(1-ζ²)]`；
- 计时器误差限：`Δ仪 = 读数×10^-5 + 0.001 s`；
- 幅频/相频曲线、频率比 `ω/ω0`、共振点、稳定时间 `exp(-t/τ)<0.01`。

### 10.5 示波器/声速/电路

- 示波器分度法：格数×分度值；周期/频率；两路时间差转相位差；
- 利萨如频率比 `fy/fx=nx/ny`；
- 声速 `v=fλ`；
- 同相点 `x_n=a+b n`，`λ=b`；
- `Δλ=t Sb`，自由度 `n-2`；
- `Δf=10 Hz`（课程指定）；
- 干燥空气/湿空气理论声速公式；
- 饱和蒸气压表线性插值；
- RC 充电 `uC=E(1-exp(-t/RC))`；
- 微分电路近似 `uR≈RC·du/dt`；
- 串联共振 `f0=1/(2π√(LC))`。

### 10.6 透镜焦距

- 薄透镜 `1/f=1/p+1/q`；
- 放大率 `β=y'/y=-q/p`；
- 共轭法 `f=(b²-a²)/(4b)`，6 次测量；课程给定 `Δa=0.25 cm`、`Δb=0.20 cm`；
- 焦距仪 `fx=(y'/y)f`；
- 平行光管焦距相对不确定度 0.3%，线距 y 相对不确定度 0.02%（可略），测微目镜单位置仪器误差 0.004 mm，因此线距的 B 分量 `√2×0.004 mm`；
- 凹透镜自准法 `f=-|F2-O2|`；
- 凹透镜转 180° 两次位置取平均以消除光心装配系统误差；
- 光具座每位置读数仪器误差 0.05 cm。

### 10.7 迈克尔逊

- 点光源光程差及明暗条件；
- 等倾 `δ=2d cosθ`；
- 中心 `2d=kλ`；
- 波长 `λ=2Δd/Δk`；
- 等厚近轴 `δ≈2d-dθ²`，近交线 `δ≈2d`；
- 白光附加光程差 `δ'=2l(n-1)`；
- `Δd=l(n-1)`，可反解 l 或 n；
- 实验页面必须高亮激光高压/强光、钠灯、光学表面、磁座操作安全提示。

## 11. 通用计算器必须支持

v1 至少支持：

- 四则运算、幂、任意次开方、指数/对数、三角函数（角度/弧度切换）、科学记数法；
- 算术平均、几何平均（扩展）、加权平均；
- 样本方差、样本标准差、均值标准误；
- 最大/最小、极差、半极差；
- 协方差、相关系数；
- 线性插值；
- OLS、过原点拟合、加权线性拟合；
- 相对偏差、百分差；
- 直接测量课程不确定度；
- GB/T A/B/合成/扩展不确定度；
- 自定义间接量不确定度传播；
- 单位换算；
- 有效数字/小数位工具。

加权平均属于通用扩展，不得标成上传课程讲义的必修公式。默认权重可选：手动权重，或独立测量 `wi=1/ui²`；若使用后者，可给出 `u(x̄w)=1/sqrt(Σwi)`，并标记其独立性假设。

## 12. UI/UX 硬性要求

- 全站采用“文本优先的实验工作台”视觉语言：克制、低装饰、清晰层级，不使用 emoji 作为导航或主要按钮图案，不使用纯图案按钮替代可读文字。计算器等领域内天然符号键除外。
- 桌面全局只允许一条左侧主导航。实验工作台可在左侧增加步骤导航，中部为数据表/图/表单，并且**最多只有一个**可折叠的“结果检查器”区域；禁止再增加第二条右侧栏、右侧上下文栏或右侧设置栏。
- 宽度不足以舒适容纳三列时，结果检查器必须移动到主工作区下方，而不是继续压缩中间数据区；移动端采用底部主导航 + 单列分步页面。
- 表格必须支持从 Excel/Sheets 粘贴 TSV、键盘 Enter/Tab 导航、增删行、撤销、列单位；增删行等操作按钮必须使用可读文字，不使用“＋/×”一类纯图案按钮。
- 公式卡必须提供“复制 LaTeX”“复制 Markdown 行内”“复制 Markdown 块”。
- 所有关键结果必须能展开“公式 → 数值代入 → 未修约值 → 修约依据 → 最终表达”。
- 图至少包含：实际物理量名称、单位、标度、数据点、必要拟合线、图名；多个系列要有清楚区分。
- 不以颜色作为唯一信息编码；遵循 WCAG AA；键盘焦点必须清晰可见；交互目标不得小于 WCAG 2.2 的 24×24 CSS px 最低要求；KaTeX 同时保留可访问文本/MathML。
- 支持 `prefers-reduced-motion`，避免非必要动画；支持亮/暗色主题，但科学图表导出默认采用白底可打印样式。
- UI 重构优先通过现有 React + CSS 完成，不为图标、动效或基础布局引入额外 UI 依赖，保持 GitHub Pages 包体和运行时开销轻量。

## 13. 数据持久化与隐私

- 默认完全本地运行，无后端；实验数据不上传。
- 使用 IndexedDB 保存项目；localStorage 只保存轻量偏好。
- 所有项目数据结构包含 `schemaVersion`，升级必须有迁移函数。
- 必须支持 JSON 项目导入/导出、CSV 数据表导入/导出。
- 清空本地数据必须二次确认。

## 14. 技术栈约束

推荐且默认：

- React + TypeScript + Vite；
- 路由使用 hash history 或等效 GitHub Pages 兼容方案；
- KaTeX；
- mathjs（安全 AST/导数/单位辅助）；
- Decimal.js（十进制修约和有效数字）；
- jStat 或经过测试的等价库（Student-t/CDF/quantile）；
- ECharts（SVG renderer 优先）；
- Zustand；
- Dexie/IndexedDB；
- Zod；
- Vitest + fast-check + Playwright。

引入新数学库前必须给出数值验证测试，不得只因为 API 方便而更换核心算法。

## 15. GitHub Pages 约束

- Vite `base` 必须支持仓库子路径。
- 不依赖服务端重写；路由方案必须在直接访问 GitHub Pages 时可用。
- 构建产物不得包含绝对 `/assets/...` 假设。
- GitHub Actions 必须跑 typecheck、unit tests、build 后再部署。
- PWA 如启用，service worker 更新策略必须避免缓存旧公式定义导致计算逻辑长期不刷新。

## 16. 目录约束

```text
src/
  app/
  core/
    numeric/
    quantity/
    sigfig/
    statistics/
    regression/
    uncertainty/
    expression/
    graph/
  standards/
    tsinghua-a1-2026/
    tsinghua-foundation-2020/
    gbt-27418-2017/
  formulas/
    registry/
    measurement/
    mechanics/
    oscillation/
    waves/
    thermal/
    electromagnetism/
    optics/
  experiments/
    tsinghua-a1-2026/
  instruments/
  components/
  features/
  persistence/
  export/
  tests/
```

禁止让某个实验组件直接依赖另一个实验组件的内部实现；共享数学逻辑必须下沉 `core/`。

## 17. 测试最低要求

### 17.1 单元测试

核心数学模块语句/分支覆盖目标 ≥ 90%。至少包含：

- 有效数字：0、负数、科学计数、尾随 0、跨数量级进位；
- 单位：m/mm、V/mV/μV、A/mA、T/mT、℃与 K；
- A 类/B 类/合成不确定度；
- t 因子典型自由度；
- 偏导传播；
- 相关量协方差传播；
- OLS 与课程 `r`；
- 加权平均；
- 线性插值；
- 每个实验的核心公式。

### 17.2 资料回归测试

必须把资料中的示例作为 golden tests，例如：

- 环体积示例最终约 `V=(9.44±0.08) cm³`；
- 绪论课螺旋测微计例必须得到资料给出的修正后结果量级/格式；
- 直线拟合例验证 a、b、r；
- GB/T 的矩形分布 `a/√3`、扩展不确定度报告例；
- 透镜、声速、阻尼的课程指定自由度规则。

### 17.3 属性测试

使用 fast-check 验证：

- 单位换算往返不改变物理量；
- 不确定度分量增大时 RSS 合成不确定度不减小；
- 线性缩放的传播结果满足尺度关系；
- 加权平均落在输入值凸包内（权重均为正时）；
- 格式化后估计值末位与不确定度末位对齐。

## 18. 内容与学术诚信

- 不生成虚假的“原始实验数据”。
- 不自动替学生编写虚构的实验现象或误差原因。
- 可以依据输入数据指出残差异常、趋势异常、单位异常，但必须描述为“数据诊断”，不把其冒充为实验事实。
- 报告导出必须保留用户可追溯的计算链和规则说明。
- 当课程规则与 GB/T 标准不同，必须在 UI 中解释差异，而不是暗中统一。

## 19. 完成定义（Definition of Done）

任何功能只有同时满足以下条件才算完成：

1. 需求与标准配置已明确；
2. 计算公式有 provenance；
3. 单位和维度有定义；
4. 有效数字/不确定度显示经过 profile 处理；
5. 单元测试覆盖典型与边界情况；
6. UI 有加载/空值/错误/移动端状态；
7. 可以导出并复现结果；
8. 文档和 `plan.md` 对应条目已更新；
9. GitHub Pages production build 可运行；
10. 不引入课程模式与 GB/T 模式的规则混算。

